language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-rocket' ]; then cd .. && eval "mv $currentfolder generator-rocket" && cd generator-rocket; fi

